### Json Schema 定義に基づいて、補完を行うように (エディタが対応していれば)

スキーマを公開しているストアは複数ある
(e.g. https://www.schemastore.org/json/)

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
```

```yaml

# yaml-language-server: $schema=https://json-schema.org/draft/2020-12/schema
```
